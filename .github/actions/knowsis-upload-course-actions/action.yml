name: "Knowsis Upload Course Action"
description: "Deploy the lesson through Web3 & NFT Storage"
inputs:
  who-to-greet: # id of input
    description: "Who to greet"
    required: true
    default: "Discovery Labs"
outputs:
  time: # id of output
    description: "The time we greeted you"
runs:
  using: "docker"
  image: "Dockerfile"
